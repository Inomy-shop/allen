# Legacy agent definitions have been removed.
#
# OrgSeedService (packages/server/src/services/org-seed.ts) is now the
# single source of truth for all built-in agents. Agents are seeded into
# the database at startup and loaded from there at execution/validation time.
#
# loadAgents() in agents-loader.ts no longer reads this file by default.
# The file is kept as a placeholder so tools that check its existence
# (e.g. path probes) do not error. It intentionally contains no agents.
agents: {}
